# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
pyyaml>=6.0
pytest>=8.0
typer>=0.15
huggingface_hub[cli]>=0.24
# Runtime dependencies for vLLM's benchmark_serving.py (see scripts/.bench-tool)
numpy>=1.24
aiohttp>=3.9
tqdm>=4.66
datasets>=2.18
transformers>=4.40
pillow>=10.0
# MiniCPM-V (and other VLMs) ship remote tokenizer/config code that imports
# torch when loaded with --trust-remote-code in benchmark_serving.py. Install
# the CPU wheel; the benchmark client only needs it for tokenization, not GPU.
torch>=2.2
